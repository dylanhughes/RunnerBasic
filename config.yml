name: Build out MySQL server and Apache Server
description: Build out MySQL server and Apache Server

# Allow for single panel fact collection.
fields:

  # Data Center field.
  - name: datacenter
    displayName: Datacenter Picker
    type: datacenters
    required: true

  # MySQL password field
  - name: mysqlpass
    displayName: MySQL Password
    type: password
    required: true

tags:
  - cloud
